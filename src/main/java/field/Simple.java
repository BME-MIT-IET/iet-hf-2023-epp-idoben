package field;
import com.main.Virologist;

public class Simple extends Field{
    /**
     * Konstruktor, beállítja az objektum nevét
     *
     * @param name a neve
     */
    public Simple(String name, int x, int  y) {
        super(name, x, y);
    }
}